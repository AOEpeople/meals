security:
    encoders:
        Xopn\MealzForZombies\ZombiesBundle\Entity\Zombie:
            algorithm:        sha1
            encode_as_base64: false
            iterations:       1

    role_hierarchy:
        ROLE_ADMIN:       ROLE_USER

    providers:
        user:
            entity: { class: XopnMealzForZombiesZombiesBundle:Zombie, property: username }

    firewalls:
        mealz:
            pattern: ^/
            anonymous: ~
            form_login:
                login_path:  XopnMealzForZombiesZombiesBundle_login
                check_path:  XopnMealzForZombiesZombiesBundle_checkLogin
            logout:
                path:   XopnMealzForZombiesZombiesBundle_logout
                target: /
        dev:
            pattern:  ^/(_(profiler|wdt)|css|images|js)/
            security: false

    access_control:
        #- { path: ^/login, roles: IS_AUTHENTICATED_ANONYMOUSLY, requires_channel: https }
