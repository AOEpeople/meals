
{% set participant = event.getParticipant(app.user.profile) %}

<div class="meal-row event" data-slug="{{ event.slug }}">
    <div class="title no-description">
        {{ 'event.title'|trans({},'messages') }}: <span>{{- event.title -}}</span>
    </div>
    {% include 'MealzMealBundle:Meal/partials:eventActionsWrapper.html.twig' %}
</div>
