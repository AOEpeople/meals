{% extends '::base.html.twig' %}

{% set wrapperClass = 'meal-form' %}

{% form_theme form _self %}

{% block datetime_widget -%}
    {{ value }}
{%- endblock datetime_widget %}

{% block body %}

    {{ MealzHeadline.create(1, 'week.create'|trans({},'general'), '', '', []) }}

    <div class="week-form">
        {{ form_start(form) }}
        <div class="week-disable">
            {{ form_widget(form.disabled) }}
        </div>

        {% for day in form.days %}
        <div class="meal">
            {# display week day and date #}
            <div class="week-day">
                <span>
                    {{ form_widget(day.dateTime) }}
                </span>
            </div>

            <div class="meal-rows-wrapper">
                {# display meals for given day #}
                {% for meal in day.meals %}
                <div class="meal-row">
                    {{ form_widget(meal) }}
                </div>
                {% endfor %}
            </div>

            <div class="week-day-action">
                {{ form_widget(day.disabled) }}
            </div>
        </div>
        {% endfor %}
        {{ form_end(form) }}
    </div>
{% endblock %}