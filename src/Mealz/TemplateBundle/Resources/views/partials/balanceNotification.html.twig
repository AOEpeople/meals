{% if is_granted('IS_AUTHENTICATED_FULLY') %}
	{% if balance < -50 %}
		<div class="fancybox">
			<p>{{ "payment.acknowledge_debts.text"|trans({'%amount%': balance}, "messages") }}</p>
			<a title="Close" class="button fancybox-item" href="javascript:;">{{ "payment.acknowledge_debts.button"|trans({}, "messages") }}</a>
		</div>
	{%  endif %}
{%  endif %}
