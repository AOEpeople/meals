type ParticipationCountData = {
    mealId:         number,
    count:          number,
    isAvailable:    boolean
};

type SlotData = {
    date:       Date,
    slotSlug:   string,
    slotCount:  number
};
